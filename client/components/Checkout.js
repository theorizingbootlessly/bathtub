import React, { Component } from 'react';

export default class Checkout extends Component {
  render() {
    return (
      <div>
        This component now exists. (Here for the sake of the link working from Cart.)
      </div>
    );
  }
}
